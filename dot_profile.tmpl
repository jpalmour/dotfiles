# ~/.profile
# Read by login sessions and (on Ubuntu) by display managers for GUI apps.
# Keep minimal and shell-agnostic.

# Ensure user paths are available to GUI apps (pipx, nvm-installed bins, etc.)
export PATH="$HOME/bin:$HOME/.local/bin:$PATH"

export LANG=en_US.UTF-8
