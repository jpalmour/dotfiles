#!/usr/bin/env bash
set -eo pipefail

# Load nvm
if [ -s "$HOME/.nvm/nvm.sh" ]; then
  . "$HOME/.nvm/nvm.sh"
  nvm use --lts
fi

# Install/upgrade AI CLI tools
npm install -g @anthropic-ai/claude-code