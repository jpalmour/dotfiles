# --- Commit signing with SSH (via 1Password) ---
[gpg]
    format = ssh
{{- /* OS-specific path to op-ssh-sign */}}
{{- if eq .chezmoi.os "darwin" }}
    ssh.program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign
{{- else if eq .chezmoi.os "linux" }}
    ssh.program = /opt/1Password/op-ssh-sign
{{- else if eq .chezmoi.os "windows" }}
    ssh.program = op-ssh-sign.exe
{{- else }}
    ssh.program = op-ssh-sign
{{- end }}

[commit]
    gpgSign = true

# Use the public key text from 1Password as the signing key
{{- $sshItem := onepasswordItemFields (default "Dev SSH Key") -}}
{{- $pub := index $sshItem "Public Key" -}}
{{- if not $pub }}{{ fail "Could not read 'public key' field from 1Password SSH Key item. Check "Dev SSH Key" item fields." }}{{ end }}
[user]
    signingkey = {{ $pub }}

# --- Platform niceties ---
{{- if eq .chezmoi.os "windows" }}
# Use Microsoft OpenSSH so 1Password’s agent works smoothly
[core]
    sshCommand = C:\\Windows\\System32\\OpenSSH\\ssh.exe
{{- end }}

[user]
    name = Joseph Palmour
    email = {{ if and .is_work_machine (ne .work_email ``) -}}{{ .work_email }}{{- else -}}{{ .personal_email }}{{- end }}
